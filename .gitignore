# Environment — never commit secrets
.env
**/.env
*.env
!.env.example
!.env*.example

# Dependencies
node_modules/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo

# Build & caches (monorepo)
# Note: do not ignore root `dist/` — some packages (e.g. frontend) track assets under dist/.
coverage/
.turbo/
.cache/
*.tsbuildinfo
.expo/
.expo-shared/

# Metro / React Native
.metro-health-check*
